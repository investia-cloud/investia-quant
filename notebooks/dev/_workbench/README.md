# Workbench notebooks

This directory contains development notebooks used by Claude Code
during implementation of new framework features.

See `CLAUDE.md` section "Notebook workflow protocol" for the
workflow rules.

Each workbench notebook follows the naming convention
`dev_<feature>.ipynb` and persists as an audit
trail of the analysis process. Workbenches are not actively
maintained after their milestone closes.
